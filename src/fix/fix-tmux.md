- must source `~/.tmux.conf` to load any change in`.tmux.conf`  
- must use correct var `\$TERM` as `export TERM=screen-256color-bc`  
- if `screen-256color-bc` cause problem with zsh/kitty dont initialise `\$TERM`
